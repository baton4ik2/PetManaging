# Pet Project Frontend

React + TypeScript frontend для системы управления питомцами.

## Технологии

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev сервер
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **Axios** - HTTP клиент

## Запуск в режиме разработки

```bash
npm install
npm run dev
```

Приложение будет доступно на http://localhost:5173

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## Структура проекта

```
frontend/
├── src/
│   ├── components/     # React компоненты
│   │   ├── OwnerForm.tsx
│   │   ├── OwnerList.tsx
│   │   ├── PetForm.tsx
│   │   ├── PetList.tsx
│   │   └── ProtectedRoute.tsx  # Защита маршрутов
│   ├── pages/          # Страницы приложения
│   │   ├── Home.tsx
│   │   ├── Owners.tsx
│   │   ├── Pets.tsx
│   │   ├── Login.tsx      # Страница входа
│   │   └── Register.tsx   # Страница регистрации
│   ├── contexts/        # React Context
│   │   └── AuthContext.tsx  # Контекст аутентификации
│   ├── services/        # API сервисы
│   │   ├── api.ts       # HTTP клиент с JWT
│   │   └── authService.ts  # Сервис аутентификации
│   ├── App.tsx         # Главный компонент
│   └── main.tsx        # Точка входа
├── Dockerfile          # Docker образ
├── nginx.conf         # Конфигурация Nginx
└── package.json
```

## Переменные окружения

Создайте файл `.env`:

```
VITE_API_URL=http://localhost:8081/api
```

## Запуск через Docker

```bash
docker-compose up frontend
```

Или весь стек:

```bash
docker-compose up
```

## Функциональность

### Аутентификация
- ✅ Регистрация новых пользователей
- ✅ Вход в систему (Login)
- ✅ Выход из системы (Logout)
- ✅ Хранение JWT токена в localStorage
- ✅ Автоматическое добавление токена в API запросы
- ✅ Защищенные маршруты (требуют аутентификации)
- ✅ Автоматический редирект на логин при 401 ошибке

### Управление данными
- ✅ Управление владельцами (CRUD)
- ✅ Управление питомцами (CRUD)
- ✅ Фильтрация питомцев по типу и владельцу
- ✅ Поиск в реальном времени
- ✅ Страница статистики

### UI/UX
- ✅ Современный UI с Tailwind CSS
- ✅ Адаптивный дизайн
- ✅ Отображение статуса аутентификации в навигации
- ✅ Обработка ошибок и валидация форм

## Аутентификация

Приложение использует JWT токены для аутентификации. После регистрации или входа, токен автоматически сохраняется и добавляется ко всем API запросам.

### Использование

1. Откройте приложение в браузере
2. Нажмите "Register" для создания аккаунта
3. Заполните форму (username, email, password)
4. После регистрации вы автоматически войдете в систему
5. Все страницы теперь доступны

### Защищенные маршруты

Все страницы кроме `/login` и `/register` защищены и требуют аутентификации. При попытке доступа без токена, пользователь будет перенаправлен на страницу входа.
