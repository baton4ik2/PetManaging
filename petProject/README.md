# Pet Project - Система управления питомцами

Pet Project - это полнофункциональное веб-приложение для управления питомцами и их владельцами, построенное на Spring Boot с React фронтендом.

## Технологии

### Backend
- **Java 17**
- **Spring Boot 3.4.0**
- **Spring Data JPA**
- **Spring Security** - безопасность и авторизация
- **JWT (JSON Web Tokens)** - аутентификация
- **PostgreSQL** (в Docker)
- **Liquibase** - для миграций базы данных
- **MapStruct** - для маппинга между Entity и DTO
- **Lombok** - для уменьшения boilerplate кода
- **Swagger/OpenAPI** - для документации API
- **Spring Validation** - для валидации данных

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev сервер
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **React Context** - управление состоянием аутентификации

### Инфраструктура
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация сервисов
- **Nginx** - веб-сервер для фронтенда

## Структура проекта

```
petProject/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── ru/akbirov/petproject/
│   │   │       ├── PetProjectApplication.java
│   │   │       ├── controller/     # REST контроллеры
│   │   │       ├── service/        # Бизнес-логика
│   │   │       ├── repository/     # Репозитории для работы с БД
│   │   │       ├── entity/         # JPA сущности
│   │   │       ├── dto/            # Data Transfer Objects
│   │   │       ├── mapper/         # MapStruct мапперы
│   │   │       └── exception/      # Исключения
│   │   └── resources/
│   │       ├── application.yaml    # Конфигурация приложения
│   │       └── db/
│   │           └── changelog/      # Liquibase миграции
│   └── test/                       # Тесты
├── frontend/                       # React приложение
│   ├── src/
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml              # Docker Compose конфигурация
├── Dockerfile                      # Dockerfile для backend
└── pom.xml                         # Maven конфигурация
```

## Быстрый старт

### Запуск через Docker (Рекомендуется) 🐳

Самый простой способ запустить приложение:

```bash
docker-compose up -d
```

Это автоматически:
- Соберет Docker образы для backend и frontend
- Запустит PostgreSQL в контейнере
- Запустит Spring Boot приложение
- Запустит React фронтенд через Nginx
- Настроит все подключения и миграции БД

**Доступ к приложению:**
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8081/api
- **Swagger UI**: http://localhost:8081/swagger-ui.html
- **PostgreSQL**: localhost:5433 (внешний порт)

**Примечание:** Для доступа к защищенным эндпоинтам требуется JWT токен. Сначала зарегистрируйтесь или войдите через фронтенд.

**Остановка:**
```bash
docker-compose down
```

**Остановка с удалением данных:**
```bash
docker-compose down -v
```

Подробные инструкции: [DOCKER.md](DOCKER.md)

## API Документация

После запуска приложения, Swagger UI доступен по адресу:
- **Swagger UI**: http://localhost:8081/swagger-ui.html
- **OpenAPI JSON**: http://localhost:8081/v3/api-docs

## Аутентификация

Приложение использует JWT (JSON Web Tokens) для аутентификации. Все защищенные эндпоинты требуют токен в заголовке `Authorization: Bearer <token>`.

### Тестовый аккаунт администратора

Для быстрого доступа к приложению можно использовать предустановленный тестовый аккаунт администратора:

- **Логин**: `admin`
- **Пароль**: `admin123`
- **Роль**: `ADMIN` (полный доступ ко всем функциям)

Этот аккаунт создается автоматически при первом запуске приложения через миграции Liquibase.

### Эндпоинты аутентификации

- `POST /api/auth/register` - Регистрация нового пользователя
  ```json
  {
    "username": "user123",
    "email": "user@example.com",
    "password": "password123"
  }
  ```

- `POST /api/auth/login` - Вход в систему
  ```json
  {
    "username": "user123",
    "password": "password123"
  }
  ```

**Ответ содержит:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "username": "user123",
  "email": "user@example.com",
  "roles": ["ROLE_USER"]
}
```

### Использование токена

Все защищенные API запросы должны содержать заголовок:
```
Authorization: Bearer <your-jwt-token>
```

## API Endpoints

> **⚠️ Важно:** Все эндпоинты ниже требуют аутентификации (JWT токен), кроме `/api/auth/**` и `/swagger-ui/**`.

### Владельцы (Owners)

- `POST /api/owners` - Создать нового владельца
- `GET /api/owners` - Получить всех владельцев
  - Параметр `search` - поиск по имени, фамилии или email
- `GET /api/owners/{id}` - Получить владельца по ID
- `PUT /api/owners/{id}` - Обновить владельца
- `DELETE /api/owners/{id}` - Удалить владельца
- `GET /api/owners/{id}/pets` - Получить всех питомцев владельца

### Питомцы (Pets)

- `POST /api/pets` - Создать нового питомца
- `GET /api/pets` - Получить всех питомцев
  - Параметры запроса:
    - `type` - фильтр по типу питомца (DOG, CAT, BIRD, FISH, RABBIT, HAMSTER, OTHER)
    - `ownerId` - фильтр по ID владельца
    - `search` - поиск по имени, породе или владельцу
- `GET /api/pets/{id}` - Получить питомца по ID
- `PUT /api/pets/{id}` - Обновить питомца
- `DELETE /api/pets/{id}` - Удалить питомца

### Статистика (Statistics)

- `GET /api/statistics` - Получить общую статистику
  - Возвращает: количество владельцев, питомцев, распределение по типам, среднее количество питомцев на владельца

## Примеры запросов

### Регистрация пользователя
```bash
curl -X POST http://localhost:8081/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user123",
    "email": "user@example.com",
    "password": "password123"
  }'
```

### Вход в систему
```bash
curl -X POST http://localhost:8081/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user123",
    "password": "password123"
  }'
```

Сохраните полученный токен для последующих запросов.

### Создание владельца (требует токен)
```bash
curl -X POST http://localhost:8081/api/owners \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "firstName": "Иван",
    "lastName": "Иванов",
    "email": "ivan@example.com",
    "phone": "+7-999-123-45-67",
    "address": "Москва, ул. Примерная, д. 1"
  }'
```

### Создание питомца (требует токен)
```bash
curl -X POST http://localhost:8081/api/pets \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "name": "Барсик",
    "type": "CAT",
    "breed": "Персидская",
    "dateOfBirth": "2020-05-15",
    "color": "Белый",
    "description": "Дружелюбный кот",
    "ownerId": 1
  }'
```

### Поиск питомцев
```bash
curl "http://localhost:8081/api/pets?search=Барсик"
```

### Получение статистики
```bash
curl http://localhost:8081/api/statistics
```

## Типы питомцев

- `DOG` - Собака
- `CAT` - Кошка
- `BIRD` - Птица
- `FISH` - Рыба
- `RABBIT` - Кролик
- `HAMSTER` - Хомяк
- `OTHER` - Другое

## Функциональность

### Backend
- ✅ CRUD операции для владельцев и питомцев
- ✅ JWT аутентификация и авторизация
- ✅ Spring Security конфигурация
- ✅ Защита API эндпоинтов
- ✅ Поиск по имени, породе, владельцу
- ✅ Фильтрация по типу и владельцу
- ✅ Статистика по питомцам
- ✅ Автоматический расчет возраста питомца
- ✅ Валидация данных
- ✅ Обработка ошибок
- ✅ Swagger документация

### Frontend
- ✅ JWT аутентификация (Login/Register)
- ✅ Защищенные маршруты
- ✅ Автоматическое добавление токена в запросы
- ✅ Управление состоянием аутентификации
- ✅ Управление владельцами (CRUD)
- ✅ Управление питомцами (CRUD)
- ✅ Поиск в реальном времени
- ✅ Фильтрация по типу и владельцу
- ✅ Страница статистики
- ✅ Адаптивный дизайн
- ✅ Обработка ошибок

## Обработка ошибок

Приложение обрабатывает следующие ошибки:
- `401 UNAUTHORIZED` - Требуется аутентификация или токен недействителен
- `403 FORBIDDEN` - Недостаточно прав доступа
- `404 NOT FOUND` - Владелец или питомец не найден
- `409 CONFLICT` - Email или username уже используется
- `400 BAD REQUEST` - Ошибки валидации данных

## Разработка

Для разработки рекомендуется использовать IDE с поддержкой:
- Lombok (нужен плагин для IDE)
- MapStruct (автоматическая генерация мапперов)

### Локальная разработка (без Docker)

Если нужно запустить приложение локально без Docker:

1. Установите PostgreSQL локально
2. Создайте базу данных:
```sql
CREATE DATABASE pet_db;
```

3. Обновите `application.yaml`:
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pet_db
```

4. Запустите backend:
```bash
mvn spring-boot:run
```

5. Запустите frontend (в отдельном терминале):
```bash
cd frontend
npm install
npm run dev
```

## Тестовые данные

При первом запуске через Docker автоматически загружаются тестовые данные:
- Тестовый аккаунт администратора (логин: `admin`, пароль: `admin123`)
- 5 владельцев
- 11 питомцев различных типов

## Документация

- [DOCKER.md](DOCKER.md) - Подробные инструкции по Docker
- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [FRONTEND.md](FRONTEND.md) - Информация о фронтенде

## Лицензия

Этот проект создан в образовательных целях.
